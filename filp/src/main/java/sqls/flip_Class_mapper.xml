<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hk.flip.Class">

	<!-- 강사들의 강의 10개만 불러오기 -->
<select id="mainclasslist" parameterType="String" resultType="ClassDto">
	<![CDATA[ 
	SELECT
	C.SEQ,
	C.CLASS_IMG, 
	C.CLASS_MEMBER_SEQ, 
	M.MEMBER_NAME AS CLASS_CREATOR_NAME, 
	M.MEMBER_PROFILE AS PROFILE_IMG, 
	NVL(R.CLASS_RATING,0) AS CLASS_MEMBER_RATING, 
	NVL(R.CLASS_REVIEW_COUNT,0) AS CLASS_REVIEW_COUNT, 
	C.CLASS_NAME, 
	C.CLASS_INFO, 
	C.CLASS_AREA, 
	D_DAY.CLASS_D_DAY, 
	NVL(PARTI.CLASS_NOW_PARTICIPATION,0) AS CLASS_NOW_PARTICIPATION 
FROM
	CLASS C,
	MEMBER M,
	(SELECT CLASS.SEQ, ROUND(AVG(REVIEW.REVIEW_RATING),1) AS CLASS_RATING,COUNT(REVIEW_CLASS_SEQ) AS CLASS_REVIEW_COUNT
			FROM REVIEW, CLASS 
			WHERE REVIEW.REVIEW_CLASS_SEQ = CLASS.SEQ
			GROUP BY CLASS.SEQ
			ORDER BY CLASS_RATING DESC) R,
   	(SELECT CLASS.SEQ,(TO_CHAR(CLASS.CLASS_SD,'YYYYMMDD')-TO_CHAR(SYSDATE,'YYYYMMDD')) AS CLASS_D_DAY FROM CLASS) D_DAY,
   	(SELECT CL.SEQ,COUNT(INC.INCLASS_CLASS_SEQ) AS CLASS_NOW_PARTICIPATION 
   		FROM INCLASS INC, CLASS CL 
   		WHERE INC.INCLASS_CLASS_SEQ = CL.SEQ 
   		GROUP BY CL.SEQ) PARTI
WHERE
	C.SEQ = R.SEQ (+)
	AND C.CLASS_MEMBER_SEQ = M.MEMBER_SEQ(+)
	AND C.SEQ = D_DAY.SEQ
	AND C.SEQ = PARTI.SEQ (+)
	AND C.CLASS_DEPA = #{department}
	AND C.CLASS_TYPE = 'C'
	AND ROWNUM <= 10
	]]>
	</select>
	
	<select id="mainwantlist" parameterType="String" resultType="ClassDto">
	<![CDATA[ 
	SELECT
	C.SEQ,
	C.CLASS_IMG, 
	C.CLASS_MEMBER_SEQ, 
	M.MEMBER_NAME AS CLASS_CREATOR_NAME, 
	DECODE(M.MEMBER_PROFILE,NULL,'testimg.png',M.MEMBER_PROFILE)AS PROFILE_IMG, 
	C.CLASS_NAME, 
	C.CLASS_INFO, 
	C.CLASS_AREA, 
	D_DAY.CLASS_D_DAY, 
	NVL(PARTI.CLASS_NOW_PARTICIPATION,0) AS CLASS_NOW_PARTICIPATION 
FROM
	CLASS C,
	MEMBER M,
   	(SELECT CLASS.SEQ,(TO_CHAR(CLASS.CLASS_SD,'YYYYMMDD')-TO_CHAR(SYSDATE,'YYYYMMDD')) AS CLASS_D_DAY FROM CLASS ORDER BY CLASS_D_DAY) D_DAY,
   	(SELECT CL.SEQ,COUNT(INC.INCLASS_CLASS_SEQ) AS CLASS_NOW_PARTICIPATION 
   		FROM INCLASS INC, CLASS CL 
   		WHERE INC.INCLASS_CLASS_SEQ = CL.SEQ 
   		GROUP BY CL.SEQ) PARTI
WHERE
	C.CLASS_MEMBER_SEQ = M.MEMBER_SEQ(+)
	AND C.SEQ = D_DAY.SEQ
	AND C.SEQ = PARTI.SEQ (+)
	AND C.CLASS_DEPA = #{department}
	AND C.CLASS_TYPE = 'W'
	AND ROWNUM <= 10
	]]>
	</select>
	<select id="mainstudylist" parameterType="String" resultType="ClassDto">
	<![CDATA[ 
	SELECT
	C.SEQ,
	C.CLASS_IMG, 
	C.CLASS_MEMBER_SEQ, 
	M.MEMBER_NAME AS CLASS_CREATOR_NAME, 
	DECODE(M.MEMBER_PROFILE,NULL,'testimg.png',M.MEMBER_PROFILE)AS PROFILE_IMG, 
	C.CLASS_NAME, 
	C.CLASS_INFO, 
	C.CLASS_AREA, 
	D_DAY.CLASS_D_DAY, 
	NVL(PARTI.CLASS_NOW_PARTICIPATION,0) AS CLASS_NOW_PARTICIPATION 
FROM
	CLASS C,
	MEMBER M,
   	(SELECT CLASS.SEQ,(TO_CHAR(CLASS.CLASS_SD,'YYYYMMDD')-TO_CHAR(SYSDATE,'YYYYMMDD')) AS CLASS_D_DAY FROM CLASS ORDER BY CLASS_D_DAY) D_DAY,
   	(SELECT CL.SEQ,COUNT(INC.INCLASS_CLASS_SEQ) AS CLASS_NOW_PARTICIPATION 
   		FROM INCLASS INC, CLASS CL 
   		WHERE INC.INCLASS_CLASS_SEQ = CL.SEQ 
   		GROUP BY CL.SEQ) PARTI
WHERE
	C.CLASS_MEMBER_SEQ = M.MEMBER_SEQ(+)
	AND C.SEQ = D_DAY.SEQ
	AND C.SEQ = PARTI.SEQ (+)
	AND C.CLASS_DEPA = #{department}
	AND C.CLASS_TYPE = 'S'
	AND ROWNUM <= 10
	]]>
	</select>
</mapper>